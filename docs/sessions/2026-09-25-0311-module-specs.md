# Session: 2026-09-25, module specs for all nine modules plus platform (Phase A)

**Milestone**: roadmap week 3. Review 2 (W8) needs MF-01 and MF-02 complete, MF-03 in progress, MF-04 and MF-05 specified, plus ERD, use case, state machine and activity diagrams and an SDD. This session wrote the specs that gate all of that. No production code, per AGENTS.md §2.1.

**Agent**: cloud session, branch `feat/module-specs-and-backend-foundation`, docs root `ignore/web-pack/treklink-docs/_docs/` (snapshot `treklink-docs@089374b`). Handoff with the local orchestrator through `_handoff/SYNC.md`.

## What was produced

| Artefact | Where |
|---|---|
| Requirements, design, tasks, api-design for `platform` (new), `auth`, `devices`, `trips`, `rentals`, `incidents`, `monitoring`, `billing`, `frontend`; `gateway-sync` brought up to date | `specs/*/` |
| 102 endpoint and contract files in the template 04 shape with Mermaid activity and sequence diagrams, plus a testing guide and index per module | `specs/*/api-design/` |
| Review 2 and SDD figures, `.mmd` and `.svg` | `_handoff/outbound/capstone/Documents/reports/sdd-diagrams/` |
| Proposed SSOT edits | `_handoff/outbound/treklink-docs/_docs/**/*.proposed.md`, `_handoff/outbound/capstone/Documents/reports/Report3_SRS-FR-catalogue.proposed.md` |

Every Mermaid block was rendered with Mermaid 12.0.0 in headless Chromium (scratchpad harness), and every `design.md` figure was sized against the 7 pt print floor; the ERD, the MF-01 and MF-03 activity diagrams and two inherited `gateway-sync` figures were re-laid out or split to clear it. api-design diagrams are screen artefacts and several are below the floor when printed; they are not part of the handbook build.

## Findings and where each one landed

| Finding | Landed in |
|---|---|
| `Device.nodeNum Int?` overflows: node numbers are unsigned 32-bit values from the MAC | `specs/platform/design.md` §3.3; devices REQ-UBI-04, AC-03; proposed risk row (C-004) |
| `gateway-sync/design.md` §2.4 queried the `Incident` table directly (module-isolation breach) and excluded `RESOLVED` from the episode lookup (contradicts E03-6) | `gateway-sync` design §2.4 and REQ-EVT-05 rewritten; `incidents` design §2.3 owns the lookup; proposed risk row |
| `IncidentAudit onDelete: Cascade` is a delete path on an append-only trail | `incidents` design §1 |
| `gateway-sync` spec still used pre-D-018 stage names and retired branch names | retagged in the same pass |
| MF-01 swimlane and MF-01 text disagree on when the device is reserved | `rentals` follows text and E01-1; C-003 question; `05-main-flows.proposed.md` |
| Q60 (escrow at booking) and MF-05 (settle at return) disagree on payment timing | both supported, switch `rentals.requireEscrowBeforeReview`; C-003 |
| BR-04 contradicts Q52 on battery blocking check-out | handover check uses the manual reading only; `06-requirements-foundation.proposed.md` |
| The prompt's "Rental lifecycle, 7 states" differs from the charter's graded Device 7-state FSM | all three FSMs delivered; SDD README note; C-003 |
| O-001: backlog drains one entry per reconnect; SOS never inferable from priority; fall and no-fix SOS never beacon; health report schema | `gateway-sync` REQ-UBI-02, REQ-UBI-09, REQ-EVT-06, REQ-EVT-12 to 16, REQ-ERR-07; `incidents` design §2.3; C-002 |
| `backend/tsconfig.json` has `strictNullChecks: false` though conventions require strict | `platform` task 1.1 |
| No frontend test runner exists | `frontend` design §6; C-003 |

## Not done, deliberately

- No code, migration or UI (Phase B waits for approval).
- The SSOT (`treklink-docs`, `capstone`) is not edited; proposals are in `_handoff/outbound/`.
- `07-clarification-answers.md` gains no block until the orchestrator answers C-003.

## Next session

Read `_handoff/SYNC.md` for `O-` answers to C-003, apply them to the specs in one pass, then start Phase B with `specs/platform/tasks.md` Phase 1.
