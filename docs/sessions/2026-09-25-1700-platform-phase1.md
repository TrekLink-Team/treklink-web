# Session: 2026-09-25, `platform` Phase 1 (backend Phase B start)

**Agent**: Claude Code (Opus 5.5), local session for LongLP, branch `feat/platform-foundation` from `origin/dev@e83e4f3`. Docs root read from `treklink-docs@origin/dev` (`6490028`), because the local `treklink-docs` checkout holds uncommitted edits and was left untouched.

**Scope**: `specs/platform/tasks.md` tasks 1.1 to 1.9, one commit each. Phase 2 waits for the leader.

## Environment limits on the authoring machine

| Missing | Consequence | Substitute |
|---|---|---|
| Docker | Local Docker Postgres 16 never ran | PGlite 0.5.8 (PostgreSQL 18.3, WASM) in the session scratchpad, served over the wire protocol for `prisma migrate deploy` |
| Python | `check_prose.py` not run directly | a line-for-line Node port of it, run on every Markdown file touched |
| `gh` | PR opened outside the CLI | see the PR |

(untested) Both migrations on PostgreSQL 16, the version `docker-compose.yml` and CI use.

## What was verified

- `npm run lint` (ESLint plus the new boundary check), `npm run typecheck` under `strict: true`, `npm test` (31 tests) and `npm run build` pass after every task.
- Starting the backend without `DATABASE_URL` exits 1 and names each invalid variable (AC-04).
- On PGlite: `prisma migrate deploy` applies both migrations; `prisma migrate diff` from the migrated database to `schema.prisma` is empty, so the raw-SQL objects do not register as drift; the exclusion constraint, both CHECKs, both partial unique indexes and the append-only trigger (AC-07) each reject the write they target; `nodeNum` stores 4294967295; all 82 seeded role grants resolve.

## Findings and where each landed

| Finding | Landed in |
|---|---|
| Prisma 6.19.3 fails every CLI command except `generate` with P1012 when `DATABASE_DIRECT_URL` is unset, which REQ-OPT-02 ("WHERE set") does not anticipate | tooling note under REQ-OPT-02; `.env.example`; PR question 10 |
| The platform append-only trigger on `gateway_events` (design §4.5) blocks the `incidentId` and `priority` updates in `gateway-sync/design.md:358`, `:362` | PR question 1 |
| `rentals/design.md:10-11` Booking and Rental enums disagree with `07-clarification-answers.md` §5 Q25, Q27 (§6 Q23 says §5 governs) | PR question 2 |
| Module Prisma blocks carry no field `@map`, so columns are camelCase; the raw-SQL comments in the trips and rentals specs use snake_case | migration raw SQL uses the real quoted names; PR question 3 |
| `tstzrange()` over `timestamp` columns is not immutable, so the exclusion constraint forces `timestamptz` on the two allocation window columns | `schema.prisma` comment; PR question 7 |
| No decision-register or spec edit was made for the questions: each needs the leader's choice, and the register lives in `treklink-docs` | PR "Questions for the leader" |

## Not done, deliberately

- Phase 2 onwards (interceptor, filter mapping `errorCode`, parameter service, audit sink, scheduler).
- The demo seed of clarification answers §6 Q41 (users, devices, trips); task 1.6 covers only the parameter registry and system roles.
- Wiring `CORS_ORIGINS`, `HTTP_BODY_LIMIT` and `SWAGGER_ENABLED` into `main.ts`; they are validated only.
- CI changes: CI does not yet apply migrations (PR question 11).

## Review round, 2026-09-26: the leader's decisions on the 17 questions

The leader verified both migrations on `postgres:16-alpine` and decided all 17 questions on PR #12. Applied in this branch, folded into the single initial migration:

| Decision | Where |
|---|---|
| 1, 4, 7, 12, 13 (gateway_events exception, FKs, timestamptz, FK indexes, no `processedAt`) | `schema.prisma`, regenerated initial migration, `specs/platform/design.md` §3.4 and §4.5 |
| 3, 14, 16 (column names in spec SQL, `confidence` and `P1_LOCATION`, tasks header, §1.2 citation, api-design/04 key) | rentals, trips, gateway-sync and platform specs |
| 10 (`DATABASE_DIRECT_URL` required) | env validation, REQ-OPT-02, `.env.example` |
| 11 (migrate deploy in CI) | `.github/workflows/ci.yml`, `prisma:deploy` script |

Re-verified on PGlite: deploy applies, drift diff empty, 100 FKs, no `timestamp` column without time zone, the `gateway_events` exception accepts only a `NULL`-to-value `incidentId` update.

**Still open**

- Decision 2 waits for TanNB's rentals spec rename; the migration is regenerated again when it lands on `dev`.
- Decision 9 belongs to auth Phase 1, which writes each missing CASL condition into the auth spec before seeding it. Nothing seeded here.
- New conflict: decision 1 rejects the two post-insert priority writes in `gateway-sync/design.md` §2.5 (beacon promotion, `retroTagPositions`). Marked in that spec, raised on PR #12.
- `GatewayEvent.gatewayId` and `Device.lastGatewayId` have no FK until it is settled whether they hold a `Gateway.id` or a `gatewayKey`.
